<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="shortcut icon" href="/Logo.jpg" type="image/x-icon">
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        /* Add top padding to prevent overlap with navbar */
        .container {
            max-width: 1200px;
            margin: 80px auto 40px auto;
            padding: 30px;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            color: #004d40;
            margin-bottom: 25px;
            font-size: 2.4rem;
        }

        .grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 20px;
        }

        .card {
            padding: 25px;
            background-color: #e0f7fa;
            border-radius: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            text-align: center;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            cursor: pointer;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
        }

        .card h3 {
            color: #00695c;
            font-size: 1.4rem;
            margin-bottom: 12px;
        }

        .card p {
            color: #555;
            margin-bottom: 15px;
            font-size: 1rem;
        }

        .button {
            background-color: #004d40;
            color: white;
            padding: 10px 18px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .button:hover {
            background-color: #003d33;
            transform: translateY(-3px);
        }

        @media (max-width: 768px) {
            .container {
                margin-top: 100px;
            }

            .grid {
                grid-template-columns: 1fr; /* Stack cards on smaller screens */
                gap: 15px;
            }
        }
    </style>
</head>

<body>
    <!-- Navbar Component -->
    <%- include('components/navbar') %>

    <div class="container">
        <h1>Welcome, <%= teacherName %>!</h1>
        <div class="grid">
            <div class="card">
                <h3>Manage Questions</h3>
                <p>Create, edit, or delete questions to keep them updated.</p>
                <button class="button" id="Upload">Manage Questions</button>
            </div>
            <div class="card">
                <h3>Student Performance</h3>
                <p>Detailed student list and statistics.</p>
                <button class="button" id="Performance">View Enrolled Students</button>
            </div>
            <div class="card">
                <h3>Student Communication</h3>
                <p>Send messages or important notifications to students.</p>
                <button class="button">Send Message</button>
            </div>
            <div class="card">
                <h3>Schedule Exams</h3>
                <p>Access schedule date, time and question set.</p>
                <button class="button" id="Schedule">Schedule</button>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        for (let buttonId in buttonUrlMap) {
            const button = document.querySelector(buttonId);
            button.addEventListener('click', function () {
                window.location.href = buttonUrlMap[buttonId];
            });
        }
    </script>
</body>

</html>
